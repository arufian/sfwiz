# Session hand-off — 2026-04-25

## What happened

Opus 4.7 reviewed phase-4-implementation.md + PoC code → returned "GO WITH CHANGES" with 6 HIGH concerns (H1–H6). Sonnet implemented M1–M6 resolving all H concerns inline.

## Milestones completed this session

| # | Tag | Summary |
|---|-----|---------|
| M1 | m01 | Scaffolding: cli.ts, launch.tsx, build.ts, biome.json, tsconfig update, sanity test, agent-sdk smoke test (H4) |
| M2 | m02 | Zod schemas: config/schema.ts, personas/types.ts, session/types.ts, scraper/types.ts, tools/types.ts |
| M3 | m03 | AgentLoop: client.ts singleton+reset, agent/types.ts (subagent:* H1), cache-hints.ts (4-breakpoint H3), loop.ts, loop.integration.test.ts (H2 mock-fetch SSE) |
| M4 | m04 | Tool gates: registry.ts, gate.ts (DestructiveOpGate), permission-mode.ts (PermissionModeGuard) |
| M5 | m05 | Core tools: ask_user.ts, fs/ (read/list/edit/write/grep), shell/run_command.ts (whitelist) |
| M6 | m06 | Config/trust: trust.ts (TrustStore+backup), load.ts, save.ts, first-run.ts, models-catalog.ts, tui/events.ts (TuiEventBus) |

## Tests

64 pass, 1 skip (live API smoke gated by ANTHROPIC_API_KEY), 0 fail. TypeScript: 0 errors.

## Critical fixes discovered during M1–M6

- **SSE format bug** (M3): Anthropic SDK `Stream.fromSSEResponse` checks `sse.event` not JSON `type` field. Fake SSE must include `event: <type>\n` line before `data:` line. Fixed in integration test.
- **Zod v4 default factory** (M2): `.default({})` fails when output type has required fields; must use `.default(() => ({ ... }))`.
- **HTTP-level mock** (M3): Use `new Anthropic({ fetch: mockFetch })` not `globalThis.fetch` override.
- **Top-level await module** (M1): `cli.ts` needs `export {}` to be treated as ES module for top-level await.
- **JSX in .ts file** (M1): Moved all JSX to `launch.tsx`; `cli.ts` stays `.ts` and calls `launch()`.
- **claude-agent-sdk identity** (M1): Confirmed it's Claude Code Agent SDK — `query()` spawns Claude Code subprocess with `AgentDefinition`. M13 reviewer+qa will run as CC subagents.

## Next: M7 — SF auth

Install: `jsforce` + `@salesforce/core` (if not present).
Create:
- `src/sf/auth.ts` — `@salesforce/core` passthrough, list orgs, get active org connection
- `src/tools/sf/sf_list_orgs.ts` — tool wrapping auth.listOrgs()
- `src/tools/sf/register.ts` — registers all SF tools into ToolRegistry
- Slash command `/orgs` in dispatcher → triggers sf_list_orgs tool
- Auto-kick `sf login web` when org list empty (with ask_user confirmation)
- Tests: `tests/sf/auth.test.ts` (mock @salesforce/core)

## Hand-off state

All changes committed and pushed. Branch: main. Remote: git@github.com:arufian/sfwiz.git.
